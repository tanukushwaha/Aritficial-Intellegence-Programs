#program to shuffle a deck of card using the modules random and draw 5cards
#import modules
import itertools,random
#make a deck of cards
deck=list(itertools.product(range(1,14),['Spade','Heart','Diamond','Club']))
print(deck)
#shuffle the cards
random.shuffle(deck)
for i in range(4):
    print(deck[i][0],'of',deck[i][1])

